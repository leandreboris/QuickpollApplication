#Database configuration
spring:
    datasource:
        password: root
        url: jdbc:mysql://localhost:3306/test
        username: root
      
    jpa:
     hibernate:
      ddl-auto: update
     show-sql: true
     
     properties:
        hibernate:
         dialect: org.hibernate.dialect.MySQL8Dialect

         
    profiles:
     active: dev
     